Quantifiers select a value multiple times.
They also decide how many times a value should be selected.

Syntax:
To define a Quantifier, we add a value inside curly braces after the sets or ranges.
For example:
    [0-9]{3}